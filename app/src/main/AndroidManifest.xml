<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.a.papermaxx">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:name=".app.MyApp"
        android:allowBackup="true"
        android:icon="@drawable/paperlogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/paperlogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Papermaxx">
        <activity
            android:name=".general.CallActivity"
            android:label="@string/title_activity_call"
            android:theme="@style/Theme.Papermaxx.NoActionBar"/>
        <activity android:name=".general.MainActivity"
            android:screenOrientation="portrait"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>